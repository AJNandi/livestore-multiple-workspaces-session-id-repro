import { Store } from "@livestore/livestore";

// NOTE: This seed function is out of date
// DO not use it
// Needs to be updated to match the new schema after adding auth
export function seed(_store: Store) {}
